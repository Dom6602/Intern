
function add(x, y) {
    const Add = x + y
    return Add

}

function sub(x, y) {
    const Sub = x - y
    return Sub
}

function mul(x, y) {
    const Mul = x - y
    return Mul
}

function del(x, y) {
    const Del = x - y
    return Del
}

module.exports.add = add
module.exports.sub = sub
module.exports.mul = mul
module.exports.del = del

